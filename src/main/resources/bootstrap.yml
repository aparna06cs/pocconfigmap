spring:
  cloud:
    kubernetes:
      config:
        enabled: true
        sources:
          - namespace: configmap-poc
            name: configmap-poc
      reload:
        enabled: true
        mode: event
        strategy: REFRESH