spring:
  cloud:
    kubernetes:
      config:
        enabled: true
        sources:
          - namespace: default
            name: configmap-poc
      reload:
        enabled: true
        mode: event
        strategy: REFRESH
management:
  endpoint:
    restart:      enabled: true